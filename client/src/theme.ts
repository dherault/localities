import { HonorableTheme } from 'honorable'

export default {
  Section: {
    Root: [
      ({ container }: any) => container && {
        width: '75%',
        marginLeft: 'auto',
        marginRight: 'auto',
      },
    ],
  },
} as HonorableTheme
